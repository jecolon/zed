pub const Opcode = enum(u8) {
    add,
    constant,
    define,
    div,
    eq,
    jump,
    jump_false,
    jump_true,
    gt,
    gte,
    load,
    logic_and,
    logic_not,
    logic_or,
    lt,
    lte,
    mod,
    mul,
    negative,
    neq,
    pop,
    scope_in,
    scope_out,
    store,
    sub,
};

const Bytecode = @This();
