pub const Opcode = enum(u8) {
    add,
    constant,
    div,
    eq,
    jump,
    jump_false,
    jump_true,
    gt,
    gte,
    logic_and,
    logic_not,
    logic_or,
    lt,
    lte,
    mod,
    mul,
    negative,
    neq,
    pop,
    scope_in,
    scope_out,
    sub,
};

const Bytecode = @This();
